/*
  Author(s): Slawomir Bankowski
  Project: mlasaf
  AUTOGENERATED CODE
*/
package com.mlasaf.dao

import anorm.{SQL, SqlParser}
import com.mlasaf.dto._
import java.util.Date


 class VAlgorithmVersionDao extends DaoBase { 

  def getVAlgorithmVersionsList() : List[VAlgorithmVersionDto] = {
   implicit val connection = getConnection();
   val dtos : List[VAlgorithmVersionDto]= SQL("select * from vAlgorithmVersion").as(anorm.Macro.namedParser[VAlgorithmVersionDto].*);
   releaseConnection(connection);
   dtos
  }
  def getVAlgorithmVersionsCount() : Long = {
   implicit val connection = getConnection();
   val cnt : Long = SQL("select count(*) as cnt from vAlgorithmVersion").executeQuery()(connection).as[Long](SqlParser.long("cnt").single)(connection);
   releaseConnection(connection);
   cnt
  }
  def getVAlgorithmVersionsLastInsertDate() : java.util.Date = {
   implicit val connection = getConnection();
   val ld : java.util.Date = SQL("select max(insertedRowDate) as lastDate from vAlgorithmVersion").executeQuery()(connection).as[java.util.Date](SqlParser.date("lastDate").single)(connection);
   releaseConnection(connection);
   ld
  }
  def getVAlgorithmVersionsLastUpdatedDate() : java.util.Date = {
   implicit val connection = getConnection();
   val ld : java.util.Date = SQL("select max(lastUpdatedDate) as lastUpdatedDate from vAlgorithmVersion").executeQuery()(connection).as[java.util.Date](SqlParser.date("lastUpdatedDate").single)(connection);
   releaseConnection(connection);
   ld
  }
  def getVAlgorithmVersionFirst() : VAlgorithmVersionDto = {
   implicit val connection = getConnection();
   val dtos : VAlgorithmVersionDto= SQL("select * from vAlgorithmVersion order by insertedRowDate asc ").as(anorm.Macro.namedParser[VAlgorithmVersionDto].*).head;
   releaseConnection(connection);
   dtos
  }
  def getVAlgorithmVersionLast() : VAlgorithmVersionDto = {
   implicit val connection = getConnection();
   val dtos : VAlgorithmVersionDto= SQL("select * from vAlgorithmVersion order by insertedRowDate desc ").as(anorm.Macro.namedParser[VAlgorithmVersionDto].*).head;
   releaseConnection(connection);
   dtos
  }
  def getVAlgorithmVersionsByField(fieldName : String, fieldValue : String) : List[VAlgorithmVersionDto] = {
   implicit val connection = getConnection();
   val dtos : List[VAlgorithmVersionDto]= SQL("select * from vAlgorithmVersion where " + fieldName + " = {fieldValue} ").on("fieldValue" -> fieldValue).as(anorm.Macro.namedParser[VAlgorithmVersionDto].*);
   releaseConnection(connection);
   dtos
  }
  def getVAlgorithmVersionByGuid(guid : Long) : VAlgorithmVersionDto = {
   implicit val connection = getConnection();
   val dtos : VAlgorithmVersionDto= SQL("select * from vAlgorithmVersion where guid = {guid} ").on("guid" -> guid).as(anorm.Macro.namedParser[VAlgorithmVersionDto].single);
   releaseConnection(connection);
   dtos
  }  
 def getDtosByAlgorithmVersionId(colValue : Long) : List[VAlgorithmVersionDto] = { 
   implicit val connection = getConnection();  
   val dtos : List[VAlgorithmVersionDto] = SQL("select * from vAlgorithmVersion where algorithmVersionId = {colValue} ").on("colValue" -> colValue).as(anorm.Macro.namedParser[VAlgorithmVersionDto].*);  
   releaseConnection(connection);  
   dtos  
 }  
 def getDtosByInsertedRowDate(colValue : java.util.Date) : List[VAlgorithmVersionDto] = { 
   implicit val connection = getConnection();  
   val dtos : List[VAlgorithmVersionDto] = SQL("select * from vAlgorithmVersion where insertedRowDate = {colValue} ").on("colValue" -> colValue).as(anorm.Macro.namedParser[VAlgorithmVersionDto].*);  
   releaseConnection(connection);  
   dtos  
 }  
 def getDtosByLastUpdatedDate(colValue : java.util.Date) : List[VAlgorithmVersionDto] = { 
   implicit val connection = getConnection();  
   val dtos : List[VAlgorithmVersionDto] = SQL("select * from vAlgorithmVersion where lastUpdatedDate = {colValue} ").on("colValue" -> colValue).as(anorm.Macro.namedParser[VAlgorithmVersionDto].*);  
   releaseConnection(connection);  
   dtos  
 }  
 def getDtosByGuid(colValue : Long) : List[VAlgorithmVersionDto] = { 
   implicit val connection = getConnection();  
   val dtos : List[VAlgorithmVersionDto] = SQL("select * from vAlgorithmVersion where guid = {colValue} ").on("colValue" -> colValue).as(anorm.Macro.namedParser[VAlgorithmVersionDto].*);  
   releaseConnection(connection);  
   dtos  
 }  
 def getDtosByAlgorithmTypeId(colValue : Long) : List[VAlgorithmVersionDto] = { 
   implicit val connection = getConnection();  
   val dtos : List[VAlgorithmVersionDto] = SQL("select * from vAlgorithmVersion where algorithmTypeId = {colValue} ").on("colValue" -> colValue).as(anorm.Macro.namedParser[VAlgorithmVersionDto].*);  
   releaseConnection(connection);  
   dtos  
 }  
 def getDtosByAlgorithmVersionName(colValue : String) : List[VAlgorithmVersionDto] = { 
   implicit val connection = getConnection();  
   val dtos : List[VAlgorithmVersionDto] = SQL("select * from vAlgorithmVersion where algorithmVersionName = {colValue} ").on("colValue" -> colValue).as(anorm.Macro.namedParser[VAlgorithmVersionDto].*);  
   releaseConnection(connection);  
   dtos  
 }  
 def getDtosByAlgorithmType_algorithmTypeId(colValue : Long) : List[VAlgorithmVersionDto] = { 
   implicit val connection = getConnection();  
   val dtos : List[VAlgorithmVersionDto] = SQL("select * from vAlgorithmVersion where algorithmType_algorithmTypeId = {colValue} ").on("colValue" -> colValue).as(anorm.Macro.namedParser[VAlgorithmVersionDto].*);  
   releaseConnection(connection);  
   dtos  
 }  
 def getDtosByAlgorithmType_guid(colValue : Long) : List[VAlgorithmVersionDto] = { 
   implicit val connection = getConnection();  
   val dtos : List[VAlgorithmVersionDto] = SQL("select * from vAlgorithmVersion where algorithmType_guid = {colValue} ").on("colValue" -> colValue).as(anorm.Macro.namedParser[VAlgorithmVersionDto].*);  
   releaseConnection(connection);  
   dtos  
 }  
 def getDtosByAlgorithmType_insertedRowDate(colValue : java.util.Date) : List[VAlgorithmVersionDto] = { 
   implicit val connection = getConnection();  
   val dtos : List[VAlgorithmVersionDto] = SQL("select * from vAlgorithmVersion where algorithmType_insertedRowDate = {colValue} ").on("colValue" -> colValue).as(anorm.Macro.namedParser[VAlgorithmVersionDto].*);  
   releaseConnection(connection);  
   dtos  
 }  
 def getDtosByAlgorithmType_lastUpdatedDate(colValue : java.util.Date) : List[VAlgorithmVersionDto] = { 
   implicit val connection = getConnection();  
   val dtos : List[VAlgorithmVersionDto] = SQL("select * from vAlgorithmVersion where algorithmType_lastUpdatedDate = {colValue} ").on("colValue" -> colValue).as(anorm.Macro.namedParser[VAlgorithmVersionDto].*);  
   releaseConnection(connection);  
   dtos  
 }  
 def getDtosByAlgorithmType_algorithmTypeName(colValue : String) : List[VAlgorithmVersionDto] = { 
   implicit val connection = getConnection();  
   val dtos : List[VAlgorithmVersionDto] = SQL("select * from vAlgorithmVersion where algorithmType_algorithmTypeName = {colValue} ").on("colValue" -> colValue).as(anorm.Macro.namedParser[VAlgorithmVersionDto].*);  
   releaseConnection(connection);  
   dtos  
 }  
 def getDtosByAlgorithmType_algorithmTypeDescription(colValue : String) : List[VAlgorithmVersionDto] = { 
   implicit val connection = getConnection();  
   val dtos : List[VAlgorithmVersionDto] = SQL("select * from vAlgorithmVersion where algorithmType_algorithmTypeDescription = {colValue} ").on("colValue" -> colValue).as(anorm.Macro.namedParser[VAlgorithmVersionDto].*);  
   releaseConnection(connection);  
   dtos  
 }  

} 
